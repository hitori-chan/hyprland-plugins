# built plugin artifacts — hyprpm builds these in its own cache on `hyprpm update`
*.so
*.tmp

# local agent instructions
CLAUDE.md
HANDOFF.md
.claude/

# local dev tooling (nested-Hyprland test harness; not a plugin)
devtools/
